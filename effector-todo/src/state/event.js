import { createEvent } from "effector";

export const addItem = createEvent();
export const toggleItem = createEvent();
export const removeItem = createEvent();
export const setItems = createEvent();
